20.5.26
- The test instructions are pseudo
- CPU class was set up and the fetch + update pipelined register functions yielded correct results. Tests:
	* Ins: 0xBC, 0x1C0, 0x6
	* Ex: 0, 0xBC, 0x1C0, 0x6
	* Actual: 0, 0xBC, 0x1C0, 0x6 !
	
- Decoding has been tested.
	* Ins: 0xBC, 0x1C0, 0x6
	* Ex: 0, 0, 0xBC, 0x1C0, 0x6
	* Actual: 0, 0, 0xBC, 0x1C0, 0x6 !
	
- Insutrctions must be managed from the read logical section of the IF_ID register and results should be placed in the write sections of the ID_EX register. 
  Previous instruction is the focus, generate signals and swap them at the end of the cycle. So, in the next iteration execution of the previous instruction can begin.
  
  e.g. ADD, SUB
	CC2: 
		- Place ADD in writePip[ID_EX].ins
		- Generate signals for 0 (providing ADD is the first 'proper' instruction)
		- 0 will not cause register or memory reading and writing.
	CC3:
		- Place SUB in writePip[ID_EX].ins
		- Generate signals for ADD (this is a non immediate instruction, and register signals must be generated)
		- Signals will provide gateways to operand supplying
	CC4:
		- ADD generated alu signals, so, compuatiion of the supplied operands can begin
		- Place SUB in writePip[ID_EX].ins
		- Generate signals for SUB
		- Signals will provide gateways to operand supplying
	...
- Signal Generation
	* Ins: 0xA00
	* Ex: 0:0, 0:0, 0x400:1, 0x400:1, 0:0
	* Ac: 0:0, 0:0, 0x400:1, 0x400:1, 0:0
	
21.5.26
- Execution is happening at the right time, in today's case the pc's value was 2 and greater.
- Although the results and instructions seem to be delayed, they're matching up perfectly. 
	
	e.g. M, M
	 execute():
		- The earlier instruction was a movement (reg -> reg) one, execute the corresponding function.
		- The mov() function will set result to the operand supplied by decode.
	...
	printStatment:
		- The operand A will remain constant because no instruction exists that can overwrite it, however, the rest of the array is zero inititalised, so instruction zero will prevail
		
	ALU RESULT: 0  INS: 0
	ALU RESULT: 0  INS: 0
	ALU RESULT: 0  INS: 2568
	ALU RESULT: -45  INS: 0
	ALU RESULT: -45  INS: 0
	ALU RESULT: -45  INS: 0

- I decided to pivot from direct to index addressing as a focus point, as index addressing allows for a greater range. LDA is now index based (not direct). 
  The combination of the base and offset produce the right result so a value at a high address could be extracted.
  
22.6.26
- The skeleton of the pipeline is done and data seems to be moving correctly. 
	
	e.g. LDA (r1, [r1+0]), MOV (r0, r1)
	
	- Outside the cycle function's while loop, I pasted this code:
	std::cout << int(regs[0]);
	
	- ... The value 549 was printed to the screen. 549 initally lived inside of data memory
	
23.5.26
- STA works correctly.
- ADD, SUB, AND, OR, ADD (IMM), SUB (IMM), OR (IMM), and AND (imm) work correctly
- Data hazards are handled correctly using forwarding means. 

	e.g. ADI r0, r1, 3 | SUI r1, r0, 1
	
	There's a dependancy bewteen ADI and SUI, because the destination of r0 is equal to the source(s) of r1. Unfortanetly, the new value of r0 is avaiable in a clock
	cycle that is later than the execution of SUI. The result of forwarding produces 9 in r1, providing that r0 is initially equal to 0 and r1 7,
	
	The next logical step is load data hazard handling.
	
24.5.26
- We would know in the reg destination and signals for a LDA instructions but it's impossible to forward the data. A stall bit should prevent executing, decoding and fetching 
  until the required data is retrieved?

26.5.26
- Load data hazard handling was confusing me, after watching a video, I was able to stiumlate the hazard detection unit and come up qith these results:
	0, 0, 1, 0... which are correct.
	
- Handle flags, make ex_mem.write = 0,  ensure pipeline progresses in cc5 (review notes)
- LDA will set resume = 1, so if stall = resume = 1, the pipeline can continue
	
  
